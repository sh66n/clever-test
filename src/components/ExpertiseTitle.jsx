import React from "react";

const ExpertiseTitle = ({ children }) => {
  return (
    <div className="text-2xl font-semibold mt-6 mb-2 text-center">
      {children}
    </div>
  );
};

export default ExpertiseTitle;
